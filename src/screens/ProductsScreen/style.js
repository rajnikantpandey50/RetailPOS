import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  container: { flex: 1, backgroundColor: "rgba(52, 152, 219, 0.2)" },
  buttonContainer: {
    margin: 10,
  },
  button: {
    width: 200,
  },
});
